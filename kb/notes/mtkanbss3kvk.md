# 一、Ascend 310P3 芯片整体架构框图

```swift
┌─────────────────────────────────────────────────────────────┐
│                  Ascend 310P3 SoC 芯片                 │
│                                                        │
│  ┌────────────┐    ┌──────────────────────────────────┐    │
│  │ ARM A55集群│   │  AI Core Cluster(8个AI Cor     │    │
│  │(8核)       │   │  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐  │    │
│  │  控制CPU + │   │  │AC0│ │AC1│ │AC2│ │AC3│ │AC4│ │    │
│  │  AI CPU   │    │  └───┘ └───┘ └───┘ └───┘ └───┘  │    ││  └────────────┘    │  ┌───┐ ┌───┐ ┌───┐             │    │
│                   │  │AC5│ │AC6│ │AC7│            │    │
│                   │  └───┘ └───┘ └───┘             │    │
│                   └──────────────────────────────────┘    │
│                                                        │
│  ┌────────────┐    ┌────────────┐    ┌────────────────┐    │
│  │ DVPP      │    │ 任务调度TS │    │ LPDDR4 内存控制器│  │
│  │图像编解码  │    │Task Scheduler│ │ Global Memory   │  │
│  │JPEG/VDEC/VENC│  └────────────┘    └────────────────┘   │
│  └────────────┘                                         │
│                                                        │
│  ┌────────────┐    ┌──────────────────────────────────┐    │
│  │ PCIe4.0   │    │ 外设：I2C/UART/SPI/GPIO/CAN等  │    │
│  │接口       │    │                                │    │
│  └────────────┘    └──────────────────────────────────┘    │
│                                                        │
│  └────────────── 片上NoC总线 ───────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 模块说明（面试背诵）

1. **=={yellow}ARM A55 8 核==**=={yellow}：==分为**=={green}控制 CPU=={yellow}**（操作系统、驱动）、**=={green}AI‑CPU：跑不适合 AI‑Core 的算子==**（少量标量、复杂逻辑算子）。
2. **=={yellow}AI‑Core Cluster：8 个 AI Core==**，达芬奇 V200，推理计算核心，矩阵 / 向量 / 标量计算。
3. **=={yellow}TS 任务调度器 Task Scheduler==**：=={green}硬件任务调度，把**算子任务分发到各个 AI Core**==。
4. **DVPP**：数字视觉预处理，图像缩放、裁剪、格式转换、JPEG 编解码、视频编解码，**硬件预处理，减少 AI‑Core 负担**。
5. **=={yellow}Global Memory(LPDDR4)==**：全局内存，存放模型权重、输入输出 feature map。
6. **=={yellow}NoC 片上网络总线==**：各个模块之间高速互联。
7. **PCIe4.0**：主机侧接口；丰富低速外设。

---

**二、AI Core内部架构图**

```sql
┌─────────────────────────────────────────────────────────────┐│                      GM 全局内存                        │
└───────┬───────────────────────┬─────────────────────────────┘
        │ MTE2                │ MTE2
        ▼                     ▼
┌──────────────────────────────────────────────────────────────────┐│                     AI‑Core                                 │
│                                                             │
│  ┌────────┐                                                  │
│  │ L1     │                                                 │
│  └───┬────┘                                                  │
│      │ MTE1                        ┌────────┐  ┌────────┐     │
│      ├────────────────────────────►  │ L0A    │  │ L0B   │     │
│      │                             └───┬────┘  └───┬────┘     │
│      │                                 │          │          │
│      │                                 ▼          ▼          │
│      │                            ┌──────────────┐            │
│      │                            │     Cube    │            │
│      │                            └──────┬───────┘            │
│      │                                   │                   │
│      │                                   ▼                   │
│      │                              ┌────────┐                │
│      │                              │ L0C    │               │
│      │                              └───┬────┘                │
│      │                                  │ FixPipe(硬件管道)   │
│      │                                  ▼                    │
│      │                           ┌────────────┐               │
│      │                           │     UB     │◄──────────┐   │
│      │                           └─────┬──────┘           │   │
│      │                                 │                 │   │
│      │                                 ▼                 │   │
│      │                            ┌──────────┐            │   │
│      │                            │  Vector  │────────────┘   │
│      │                            └──────────┘                │
│      │                                                       ││  ┌────────┐                                                   ││  │ Scalar │  <── 指令控制 Cube / Vector / MTE1/2/3            │
│  └────────┘                                                   │
│                                                              │
└───────────────────────────┬────────────────────────────────────────┘                          │ MTE3
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                      GM 全局内存                        │
└─────────────────────────────────────────────────────────────┘
```

---

### AI‑Core 内部组件

1. **=={yellow}Scalar Unit（标量单元）==**：AI‑Core 内部小 CPU；做循环、分支、地址计算，向 Cube、Vector、DMA 发射指令；**控制整个 AI Core 运行**。
2. **=={yellow}Cube Unit（矩阵单元，脉动阵列）==**：深度学习卷积 / 矩阵乘核心；FP16/INT8 矩阵乘；算力大头。
3. **=={yellow}Vector Unit（向量单元）==**：向量运算；激活函数、归一化、Pooling 等；SIMD 向量计算。
4. **=={yellow}DMA(MTE1/MTE2/MTE3)==**：内存搬运引擎；Global Memory 和 Local Memory 之间搬数据；计算和数据搬运可以**流水线并行（计算访存重叠）**。
5. **=={yellow}Local Memory==**=={yellow}：==AI‑Core 私有的高速片上存储（L0A/L0B/L0C 给 Cube；L1 通用）；速度远高于 Global Memory；算子优化核心就是尽量把数据放在 Local Memory，减少 DDR 访问。

---

**=={green}存储层级（由外→内）：==**

**=={yellow}GM 全局内存 (LPDDR4)==**

**→** **=={yellow}L2== (片上二级缓存，AI‑Core 集群共享)**

**→=={yellow}L1 Buffer== ：****AI‑Core 私有**

**→ =={yellow}L0A/L0B/L0C、UB== (Unified Buffer)**

 **=={green}MTE 搬运引擎：==**

- **=={yellow}MTE2：读方向：GM/L2 → AI‑Core 内部 (L1/UB)==**
- **=={yellow}MTE1：内核搬运：L1 → L0A / L0B（Cube 输入）==**
- **=={yellow}MTE3：写方向：UB → L1 / L2 / GM==**
